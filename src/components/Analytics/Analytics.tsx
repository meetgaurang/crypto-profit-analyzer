import React from 'react';
import { Switch, Route, MemoryRouter, useHistory } from 'react-router-dom';
//import { Welcome } from './Analytics.styles';

export const Analytics = () => {
    const history = useHistory();
    return (
        <>
            Analytics
        </>
    );
}
